name: Notify Site on Push (disabled — archived repo)

# Legacy OC repo is frozen at v2.40.02-final. Hub sync disabled.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "LSP_D-planner is archived; use LSP_D-planner-plus for site sync."
